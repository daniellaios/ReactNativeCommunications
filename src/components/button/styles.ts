import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    padding: 15,
    backgroundColor: 'black',
    marginHorizontal: 15,
    marginBottom: 10,
    borderRadius: 20,
    alignItems: 'center',
  },
  text: {
    color: 'white',
  },
});
